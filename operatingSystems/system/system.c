#include <sys/wait.h>
#include <unistd.h>

int main()
{
    pid_t sysPid;
    // Fork a new process
    pid = fork(void);
    
    if (pid == -1)
        print("Failed\n")
    else if ( sysPid == )

    return 0;
}
